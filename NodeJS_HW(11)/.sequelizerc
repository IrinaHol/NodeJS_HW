const path = require('path');

module.exports = {
'config': path.join(process.cwd(), 'configs', 'sequelize.config.js'),
'model-path': path.join(process.cwd(), 'dataBase', 'MySQL', 'models'),
'seeders-path': path.join(process.cwd(), 'dataBase', 'MySQL', 'seeders'),
'migrations-path': path.join(process.cwd(), 'dataBase', 'MySQL', 'migrations')
}
